pipeline {
    agent any
    environment {
      app = 'Davids app'
      path = 'files/file1.txt' 
    }

    stages {
        stage('Project Info') {
            steps {
                sh '''
                    ls
                '''
            }
        }
    }
}