pipeline {
    agent any
    environment {
        app = 'Davids App'
        path = '/files/File1.txt'
    }
    stages {
        stage('Starting Massage') {
            steps {
                sh '''
                echo "Building $app"
                '''
            }
        }
    }
}
