pipeline {
    agent any
    environment {
        app = 'Davids App'
        file = 'File1.txt'
    }
    stages {
        stage('Starting Massage') {
            steps {
                sh '''
                echo "Building $app"
                '''
            }
        }
    }
}
